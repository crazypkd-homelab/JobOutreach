services:
  joboutreach:
    image: ghcr.io/dharun/joboutreach:latest
    build:
      context: .
      dockerfile: ${JO_DOCKERFILE:-Dockerfile.slim}
    ports:
      - "${JO_PORT:-8787}:8787"
    volumes:
      - ./data:/data
    restart: unless-stopped
